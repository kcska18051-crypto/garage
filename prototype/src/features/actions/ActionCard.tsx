import { Link } from 'react-router-dom'
import { formatActionPeriod, getActionDeadline, type StoreAction } from '../../data/actionsData'
const endLabel=(value:string)=>new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'long',year:'numeric'}).format(new Date(`${value}T12:00:00`))
export function ActionCard({action,completed=false}:{action:StoreAction;completed?:boolean}){const content=<><div className={`action-card__media action-card__media--${action.cardImage}`} aria-hidden="true"><i/><b/></div><div className="action-card__body"><p className="action-card__period">{formatActionPeriod(action)}</p><h2>{action.title}</h2><p>{action.shortDescription}</p><strong>{completed?`Акция завершена ${endLabel(action.endDate)}`:getActionDeadline(action.endDate)}</strong>{completed?null:<span>Подробнее →</span>}</div></>;return completed?<article className="action-card action-card--completed" data-testid="completed-action-card">{content}</article>:<Link className="action-card" data-testid="active-action-card" to={`/actions/${action.slug}`}>{content}</Link>}
